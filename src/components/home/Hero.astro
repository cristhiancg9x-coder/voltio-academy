---
// src/components/home/Hero.astro
import { ArrowRight, BookOpen, Cpu, ShieldCheck } from 'lucide-react';
---

<section class="relative pt-28 pb-32 overflow-hidden">
  
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 relative z-10">
    
    <div class="grid grid-cols-1 lg:grid-cols-2 gap-12 items-center">
      
      {/* COLUMNA IZQUIERDA: Texto de Venta (Copywriting) */}
      <div class="space-y-8">
        
        {/* Badge superior */}
        <div class="inline-flex items-center gap-2 px-3 py-1 rounded-full border border-volt-primary/30 bg-volt-primary/5 backdrop-blur-sm">
          <span class="relative flex h-2 w-2">
            <span class="animate-ping absolute inline-flex h-full w-full rounded-full bg-volt-primary opacity-75"></span>
            <span class="relative inline-flex rounded-full h-2 w-2 bg-volt-primary"></span>
          </span>
          <span class="text-volt-primary text-xs font-display tracking-wider uppercase">Plataforma Educativa V.1.0</span>
        </div>

        <h1 class="text-5xl md:text-7xl font-display font-bold leading-tight text-white">
          La Energía del <br/>
          <span class="text-transparent bg-clip-text bg-gradient-to-r from-volt-primary to-volt-secondary">
            Conocimiento
          </span>
        </h1>

        <p class="text-xl text-slate-400 max-w-lg leading-relaxed">
          Formación técnica profesional en electricidad. Desde circuitos básicos hasta automatización industrial. <span class="text-white font-semibold">Sin teoría aburrida.</span>
        </p>

        <div class="flex flex-wrap gap-4">
          <a href="/cursos" class="group relative px-8 py-4 bg-volt-primary text-volt-dark font-bold rounded-lg overflow-hidden transition-all hover:scale-105 hover:shadow-[0_0_30px_rgba(0,240,255,0.4)]">
            <span class="relative z-10 flex items-center gap-2">
              Ver Cursos <ArrowRight className="w-5 h-5 group-hover:translate-x-1 transition-transform" />
            </span>
            {/* Efecto de brillo al pasar el mouse */}
            <div class="absolute inset-0 bg-white/20 translate-y-full group-hover:translate-y-0 transition-transform duration-300"></div>
          </a>
          
          <a href="/blog" class="px-8 py-4 border border-slate-700 text-slate-300 rounded-lg hover:border-volt-primary hover:text-volt-primary transition-colors backdrop-blur-sm">
            Leer Artículos
          </a>
        </div>
      </div>

      {/* COLUMNA DERECHA: Tarjetas de Características (Estilo HUD) */}
      <div class="relative">
        <div class="absolute inset-0 bg-gradient-to-tr from-volt-primary/20 to-volt-secondary/20 blur-[60px] rounded-full"></div>
        
        <div class="grid gap-5 relative z-10">
            
            {/* Tarjeta 1 */}
            <div class="p-6 rounded-2xl bg-volt-dark/80 border border-white/10 backdrop-blur-md hover:border-volt-primary/50 transition-colors group">
                <div class="h-12 w-12 bg-volt-secondary/20 rounded-lg flex items-center justify-center mb-4 group-hover:scale-110 transition-transform duration-300">
                    <BookOpen className="text-volt-secondary w-6 h-6" />
                </div>
                <h3 class="text-xl font-bold text-white mb-2 font-display">Teoría Práctica</h3>
                <p class="text-slate-400 text-sm">Guías paso a paso explicadas por ingenieros senior.</p>
            </div>

            {/* Tarjeta 2 (Desplazada) */}
            <div class="p-6 rounded-2xl bg-volt-dark/80 border border-white/10 backdrop-blur-md hover:border-volt-primary/50 transition-colors ml-0 lg:ml-12 group">
                <div class="h-12 w-12 bg-volt-primary/20 rounded-lg flex items-center justify-center mb-4 group-hover:scale-110 transition-transform duration-300">
                    <Cpu className="text-volt-primary w-6 h-6" />
                </div>
                <h3 class="text-xl font-bold text-white mb-2 font-display">Tecnología Actual</h3>
                <p class="text-slate-400 text-sm">Domótica, Energías Renovables y Normativa 2025.</p>
            </div>

            {/* Tarjeta 3 */}
            <div class="p-6 rounded-2xl bg-volt-dark/80 border border-white/10 backdrop-blur-md hover:border-volt-primary/50 transition-colors group">
                <div class="h-12 w-12 bg-volt-accent/20 rounded-lg flex items-center justify-center mb-4 group-hover:scale-110 transition-transform duration-300">
                    <ShieldCheck className="text-volt-accent w-6 h-6" />
                </div>
                <h3 class="text-xl font-bold text-white mb-2 font-display">Seguridad Primero</h3>
                <p class="text-slate-400 text-sm">Aprende a trabajar sin riesgos. Normas de seguridad industrial.</p>
            </div>

        </div>
      </div>

    </div>
  </div>
</section>