---
// src/pages/contacto.astro
import BaseLayout from '../layouts/BaseLayout.astro';
import { Mail, MapPin, Phone, Send, MessageSquare } from 'lucide-react';
---

<BaseLayout title="Contacto" description="Ponte en contacto con el equipo de VoltioAcademy">
  
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-16 mt-10">
    
    <div class="grid grid-cols-1 lg:grid-cols-2 gap-16">
        
        {/* COLUMNA IZQUIERDA: Información */}
        <div>
            <span class="text-volt-primary font-mono text-xs tracking-widest uppercase mb-2 block">Soporte 24/7</span>
            <h1 class="text-4xl md:text-5xl font-display font-bold text-white mb-6">
                Hablemos de <br/>
                <span class="text-transparent bg-clip-text bg-gradient-to-r from-volt-primary to-volt-secondary">Energía</span>
            </h1>
            <p class="text-slate-400 text-lg mb-12">
                ¿Tienes dudas sobre un curso? ¿Encontraste un error en una norma? 
                Estamos aquí para ayudarte a potenciar tu carrera.
            </p>

            <div class="space-y-8">
                
                <div class="flex items-start gap-4">
                    <div class="p-3 bg-volt-dark/50 border border-white/10 rounded-lg">
                        <Mail className="w-6 h-6 text-volt-primary" />
                    </div>
                    <div>
                        <h3 class="text-white font-bold text-lg">Correo Electrónico</h3>
                        <p class="text-slate-400 text-sm mb-1">Para consultas generales y soporte.</p>
                        <a href="mailto:hola@voltioacademy.lat" class="text-white hover:text-volt-primary transition-colors font-mono">hola@voltioacademy.lat</a>
                    </div>
                </div>

                <div class="flex items-start gap-4">
                    <div class="p-3 bg-volt-dark/50 border border-white/10 rounded-lg">
                        <MapPin className="w-6 h-6 text-volt-secondary" />
                    </div>
                    <div>
                        <h3 class="text-white font-bold text-lg">Oficina Central</h3>
                        <p class="text-slate-400 text-sm mb-1">Base de operaciones.</p>
                        <p class="text-white font-mono">Arequipa, Perú</p>
                    </div>
                </div>

                <div class="flex items-start gap-4">
                    <div class="p-3 bg-volt-dark/50 border border-white/10 rounded-lg">
                        <MessageSquare className="w-6 h-6 text-green-400" />
                    </div>
                    <div>
                        <h3 class="text-white font-bold text-lg">Comunidad</h3>
                        <p class="text-slate-400 text-sm mb-1">Únete a nuestro canal de Discord.</p>
                        <a href="#" class="text-white hover:text-green-400 transition-colors font-mono">discord.gg/voltioacademy</a>
                    </div>
                </div>

            </div>
        </div>

        {/* COLUMNA DERECHA: Formulario */}
        <div class="relative">
            <div class="absolute -inset-1 bg-gradient-to-r from-volt-primary to-volt-secondary rounded-2xl blur opacity-20 pointer-events-none"></div>
            
            <div class="relative bg-volt-dark/80 border border-white/10 backdrop-blur-xl rounded-2xl p-8 md:p-10">
                <h3 class="text-2xl font-display font-bold text-white mb-6">Envíanos un mensaje</h3>
                
                {/* NOTA: Aquí conectaremos Formspree más adelante.
                   Por ahora es un formulario HTML estándar.
                */}
                <form class="space-y-6">
                    
                    <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                        <div class="space-y-2">
                            <label class="text-xs font-bold text-slate-400 uppercase tracking-wider">Nombre</label>
                            <input type="text" placeholder="Tu nombre" class="w-full bg-black/50 border border-white/10 text-white p-3 rounded-lg focus:border-volt-primary focus:ring-1 focus:ring-volt-primary outline-none transition-all" />
                        </div>
                        <div class="space-y-2">
                            <label class="text-xs font-bold text-slate-400 uppercase tracking-wider">Apellido</label>
                            <input type="text" placeholder="Tu apellido" class="w-full bg-black/50 border border-white/10 text-white p-3 rounded-lg focus:border-volt-primary focus:ring-1 focus:ring-volt-primary outline-none transition-all" />
                        </div>
                    </div>

                    <div class="space-y-2">
                        <label class="text-xs font-bold text-slate-400 uppercase tracking-wider">Email</label>
                        <input type="email" placeholder="tu@email.com" class="w-full bg-black/50 border border-white/10 text-white p-3 rounded-lg focus:border-volt-primary focus:ring-1 focus:ring-volt-primary outline-none transition-all" />
                    </div>

                    <div class="space-y-2">
                        <label class="text-xs font-bold text-slate-400 uppercase tracking-wider">Asunto</label>
                        <select class="w-full bg-black/50 border border-white/10 text-slate-300 p-3 rounded-lg focus:border-volt-primary focus:ring-1 focus:ring-volt-primary outline-none transition-all">
                            <option>Consulta sobre Cursos</option>
                            <option>Soporte Técnico</option>
                            <option>Prensa / Colaboraciones</option>
                            <option>Otro</option>
                        </select>
                    </div>

                    <div class="space-y-2">
                        <label class="text-xs font-bold text-slate-400 uppercase tracking-wider">Mensaje</label>
                        <textarea rows="4" placeholder="¿En qué podemos ayudarte?" class="w-full bg-black/50 border border-white/10 text-white p-3 rounded-lg focus:border-volt-primary focus:ring-1 focus:ring-volt-primary outline-none transition-all resize-none"></textarea>
                    </div>

                    <button type="submit" class="w-full py-4 bg-volt-primary text-volt-dark font-bold rounded-lg hover:bg-white transition-all shadow-[0_0_20px_rgba(0,240,255,0.3)] flex items-center justify-center gap-2">
                        <Send className="w-5 h-5" /> Enviar Mensaje
                    </button>

                </form>
            </div>
        </div>

    </div>

  </div>
</BaseLayout>   