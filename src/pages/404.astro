---
// src/pages/404.astro
import BaseLayout from '../layouts/BaseLayout.astro';
import { AlertTriangle, Home, ArrowRight } from 'lucide-react';
---

<BaseLayout title="Error 404 - Cortocircuito" description="Página no encontrada">
  
  <div class="min-h-[80vh] flex items-center justify-center px-4 relative overflow-hidden">
    
    {/* Fondo animado de error */}
    <div class="absolute inset-0 bg-red-500/5 z-0"></div>
    <div class="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-96 h-96 bg-red-500/20 blur-[150px] rounded-full pointer-events-none animate-pulse"></div>

    <div class="relative z-10 text-center max-w-2xl mx-auto">
        
        <div class="inline-flex items-center justify-center p-6 bg-volt-dark border border-red-500/30 rounded-full mb-8 shadow-[0_0_50px_rgba(239,68,68,0.3)]">
            <AlertTriangle className="w-16 h-16 text-red-500" />
        </div>

        <h1 class="text-6xl md:text-8xl font-display font-bold text-white mb-4 tracking-tighter">
            404
        </h1>
        
        <h2 class="text-2xl md:text-3xl font-bold text-red-500 mb-6 font-display uppercase tracking-widest">
            ¡Cortocircuito!
        </h2>

        <p class="text-slate-400 text-lg mb-10 leading-relaxed">
            Parece que el cable que buscas está desconectado o la página ha sufrido una sobrecarga y no existe.
            Revisa la URL o vuelve a la central.
        </p>

        <div class="flex flex-col sm:flex-row gap-4 justify-center">
            <a href="/" class="px-8 py-3 bg-volt-primary text-black font-bold rounded-lg hover:bg-white transition-all flex items-center justify-center gap-2">
                <Home className="w-5 h-5" /> Ir al Inicio
            </a>
            <a href="/blog" class="px-8 py-3 border border-slate-700 text-slate-300 font-bold rounded-lg hover:border-volt-primary hover:text-volt-primary transition-all flex items-center justify-center gap-2">
                Ver Artículos <ArrowRight className="w-5 h-5" />
            </a>
        </div>

    </div>

  </div>

</BaseLayout>